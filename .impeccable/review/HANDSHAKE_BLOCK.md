# Paste-ready block for AGENT_HANDSHAKE.md

Insert the LOCKED entry into the **LOCKED CONTRACT REGISTRY** section and the
ledger entry under **Model A (Claude) Blueprint & Directives**.

---

## For the LOCKED CONTRACT REGISTRY

```markdown
* `[LOCKED]` `Dispatcher Console Route-Board Design System`:
  - **Token home**: `src/styles/tailwind.css` `@theme`. Additive to the Owner portal's tokens; nothing existing was changed. Do not hardcode any value this layer names.
  - **Type: seven roles, no other sizes.** `--text-micro` (12px/600/+0.06em, uppercase label tier), `--text-label` (13px/600), `--text-body` (14px/400, the only 400 role), `--text-data` (15px/600, money/quantity/timer/route number, tabular), `--text-panel` (17px/700), `--text-title` (22px/700), `--text-board` (34px/700, read-across-a-room counts). Each role carries its own line-height, weight and tracking, so a role cannot ship at four weights again. **Floor is 12px. There is deliberately no token below `--text-micro` and no third ink level.** Replaced 438 inline font-size declarations across ten sizes, 55% of them arbitrary pixels down to 9px.
  - **Palette**: `--color-board-field` #0F2035 (the navy field; was hardcoded 8 times and was never a token), `--color-board-field-deep` #0B132B, `--color-board-trim` #8494A8, `--color-board-ground` #F2EEE4 (bone working ground), `--color-board-plate` #FFFFFF. Ink is two levels only: `--color-ink` #16202E, `--color-ink-muted` #53606F, both tinted with the field's hue, never dead grey.
  - **SUGO red has exactly one legal meaning on this surface: you must act.** It carries the primary action and the states demanding a decision, and nothing else. Three values because one red cannot clear AA everywhere, and the ratios are measured not assumed: `--color-signal` #DC2626 (4.83:1 under white text; #E53935 measures 4.23:1 and FAILS at a 17px/700 button label), `--color-signal-deep` #C62828 (hover), `--color-signal-on-field` #FF8A85 (7.22:1 on navy, where #E53935 is only 3.89:1).
  - **Status law**: one fill and one ink per meaning, five meanings — `waiting` (someone else owes the next move), `moving` (in motion, nothing owed), `done` (finished well), `closed` (ended without completing), `act` (you must act). **A status may never be carried by colour alone**: `src/portals/dispatcher/lib/statusPresentation.ts` pairs every tone with a label and a CSS-drawn mark, and it maps all 15 real API status strings. An unrecognised status resolves to neutral and is never guessed.
  - **Line weight is two tokens in a fixed relationship**: `--color-hairline` (rules INSIDE a plate) and `--color-edge` (a plate's own boundary), plus `--color-field-line` on navy. **A surface's boundary is always heavier than any rule inside it; nothing may invert that.** Replaced five ad-hoc opacities (/25 /30 /40 /50 /60) chosen per call site, one of which did invert the relationship.
  - **Geometry** (AGENTS.md 8.2): `--radius-plate` 10px cards and controls, `--radius-trim` 6px inner marks, `--radius-modal` 16px modals. Pills (`rounded-full`) are for status chips and numbered markers only.
  - **One elevation step**: `--shadow-plate`. Plus `--shadow-field`, a single inset top hairline used only on navy plates. No second level, no coloured halo, no `backdrop-blur` anywhere in the console.
  - **Motion**: `--ease-board` is the one curve for every state change on the surface. `--animate-typing-dot` is opacity-only and honours `prefers-reduced-motion`.
  - **`src/styles/dispatch.css` is scoped to `[data-surface="dispatch"]` and is deliberately UNLAYERED**, so it outranks Tailwind utilities: that is what lets one focus ring replace per-component focus styles across 31 files. The ground and ink declarations are the exception and sit in `@layer base` so a component's own `bg-*` can win — without that, every dialog carrying `data-surface` rendered on the bone ground regardless of the class on it. **Anything added to this file that a component may need to override must go in `@layer base`.**
  - **`data-surface="dispatch"` is required on any portalled content** (dialogs, modals, full-screen overlays). They render at `document.body`, outside the console element, so without it they get no focus ring, no themed caret or scrollbar, no tabular figures, and icons at lucide's 2px default.
  - **Icon stroke is 1.5, set once in CSS.** lucide-react writes `stroke-width` as a presentation attribute, which any CSS declaration outranks, so all 107 icons are set from one rule with no call-site edits. `data-stroke` on an icon is the only sanctioned opt-out. `data-portal="dispatch"` extends the rule to the navigation rail and carries nothing else.
  - **`src/lib/utils.ts` must declare every new named `--text-*`, `--shadow-*` and `--radius-*` role in `extendTailwindMerge`.** Undeclared roles are misclassified and `cn()` either drops them silently or emits two conflicting classes and lets stylesheet order decide. Measured, not theoretical: `cn("text-board", "text-board-plate")` rendered only the colour, so every board figure sat at the inherited 16px/400 while the token was perfectly correct. Nothing in `tsc`, `vite build` or the design detector reports this.
  - **Primitive contracts**, `src/portals/dispatcher/components/ui/`: `PanelShell` owns the three-zone contract (pinned header, pinned controls, exactly one scroller, pinned footer) and ALL viewport math — panels must never do their own `h-[calc(100vh-Npx)]`. `PanelState` renders loading/error/empty from one contract and **orders `error` before `isEmpty`, which is what makes a false all-clear structurally impossible**. `Field` generates the input id via `useId` and cannot be rendered without wiring the label. `DispatcherCard.Region` replaces nested cards; `DispatcherCard.Label` is the label-and-rule mark. `DispatcherButton` sizes all clear a 36px target. `StatusChip` is the only correct way to render an errand status.
  - **The portal root is `h-screen overflow-hidden`, not `min-h-screen`.** A minimum is a floor, so content pushed the wrapper past the viewport and the PAGE scrolled, taking the board band and every panel title with it — defeating the pinned-header contract (AGENTS.md 8.12) on every tab. Do not change this back.
  - **The navigation rail is excluded** from this system by instruction; its alignment contract is recorded below and stays untouched.
```

---

## For Model A (Claude) Blueprint & Directives

```markdown
* **Status**: [Claude Synchronized 2026-09-16]
* **Dispatcher Console Redesign (Impeccable, route-board direction, seed f2716b25)**:
  - Full redesign of the console under the recorded direction contract at `.impeccable/surfaces/src-portals-dispatcher-dispatcherportal-tsx.md`. Product truth, props, handlers, data hooks, Socket.io listeners and Firebase subscriptions preserved exactly; every change is presentational or an additive hook field.
  - **Five fabrications deleted.** A hardcoded `₱85` fee fallback; a fake `09123456789` phone number, fixed at the mapper in `useDispatcherPortal` rather than at the view; a permanent "GPS Signal Live" label that never checked presence; a hardcoded `"You"`; and `formatPeso(Number(message.deliveryFee || 50))` in `ChatBubble`, which quoted a customer **fifty pesos out of nowhere** in the card whose job is stating what they will be charged. A sixth was found during the finish round: the inspector rendered `₱0.00` at data scale for any item without a price, which is every item before a rider files a receipt, while the region header showed the estimate — so the rows contradicted the total above them.
  - **The false all-clear is gone and cannot return.** `useOpenExceptions.reload` had no try/catch at all, so a rejection hung the panel on "Checking recent errands..." forever; and because `apiService.getOpenExceptions` returns null on failure, the old `if (data)` guard treated a dead API as nothing to report and rendered "Everything reconciles. Nothing is waiting on you." over unreconciled money. Null now means the request failed, never that the shift is quiet; the all-clear is gated on `isConfirmedClear` (loaded AND no error AND genuinely empty); and `PanelState` orders error before empty so no panel can make the mistake again.
  - **Additive hook fields only**: `loadError`, `actionError`, `dismissActionError`, `isConfirmedClear`. `alert()` is gone from the console, along with the unguarded post-alert fallback write that could throw a second time with nothing to catch it. `handleUpdateStatus` remains available from the hook; only its dead prop plumbing was removed.
  - **Anti-happy-path fixes**: the rider-message composer awaits its Firebase write and clears only on success (and `push` on the `/meta` node became `set`, which was appending a child per message to a node holding one record); `OrderChatScreen` no longer calls `onClose()` when the PATCH failed; `NotificationBell` reverts its optimistic read flip; `ExceptionEvidence` stops coercing a failed fetch to `[]`, which had it asserting "No photo was ever captured on this errand"; clipboard writes are awaited before claiming success.
  - **Draft preservation** via `src/portals/dispatcher/lib/useDraft.ts`, keyed by row and by errand id. Fixes the exception queue's single shared `reason` string (opening row B wiped row A's half-typed justification, which becomes the audit trail for money held against a customer) and the inspector's never-reset `declineReason` (a reason typed against errand A stayed submittable against errand B). **Still open**: the profile form's seven fields, deliberately, for the reasons recorded in that file.
  - **Accessibility**: one themed `:focus-visible` ring for the console, where there had been zero focus styles across 31 component files; all seven profile labels wired through `Field` (none of the seven pairings existed in the markup); the notification popover given Escape, focus management, `aria-expanded` and a real accessible name; every control at a 36px minimum, including the stage-3 and stage-1 item rows whose selects were ~22px and steppers 19px. WCAG AA verified across 22 measured pairs.
  - **Copy**: em dashes removed from every user-facing string (21 in `order-chat/copy.ts` alone); all 10 emoji removed; four strings that assumed the customer was female corrected to they/them, since the system is never told a customer's gender.
  - **Verification ledger**: `npx tsc --noEmit` 0 errors; `npm run build` clean; `impeccable detect` 0 findings; 0 unused locals/parameters/exports and 0 unreferenced `*Props` members in the console; 0 dead overridden classes across 779 static `className` strings; 0 emoji, em dashes, gradients, `backdrop-blur`, `active:scale`, synthetic italics or heavy shadows across 51 files. There is no test infrastructure in this repo, so this is the whole of the automated safety net.
  - **Unverified, and it matters**: the API is unreachable from the dev machine (CORS against `api.sugoonthego.online`, 401 with stale seed credentials locally), so no state in this rebuild has been seen against production records. Populated states were verified through fixtures that render the real components with fixed data. Screenshots are in `.impeccable/review/`; note that Chrome's headless window clamps to a 500px minimum on this machine, so the 390px case is measured numerically rather than photographed.
  - **Finish review ran and returned `fix`, and all eight findings were acted on.** Reviewer spawned fresh with no inherited transcript; brief at `.impeccable/review/HANDOFF.md`. The two that mattered: the route-board world reached exactly ONE of seven tabs, because six of them route through `PanelShell` and its header was a sentence-case title on bone (the stated red line for this whole redesign, surviving in a shared primitive I verified surface-by-surface and never looked at collectively); and the direction contract's named signature interaction, the destination band re-signing to the selected run, existed only as a comment claiming it. Both are built. Also fixed: the stage rail was gated so it vanished on an incoming run, which is the DEFAULT first viewport; `StageList` was five identical cards calling itself a rail; the route plate never reached board scale; a duplicate `SidebarTrigger` sat 35px from the rail's own; the band's navy was the identical `#0F2035` the rail paints across a 16px gutter; one failure sentence rendered up to four times on one screen, including over an unrelated profile form; panel states were centred in ~700px of empty bone; and a red-ink Decline sat beside the red primary, putting act-red on a second meaning. The reviewer disagreed with one of my deliberately-open items and was right: the profile draft question splits into four identity fields (now drafted, keyed by user id, via a `seed` that fills from a fetch only when nothing is half-typed and never writes to the store) and three password fields (still not drafted, and should not be).
  - **`DESIGN.md` at the repo root, with `.impeccable/design.json` beside it.** Every token the sidecar names was cross-checked against `tailwind.css`; nothing invented. Written by hand rather than by the documenter agent, which died on a session rate limit before writing anything.
  - **Not done, and it was asked for**: the finish reviewer also wanted each panel's primary list routed through the errand-plate treatment, not just the panel headers. That is a fourth rebuild of four surfaces and belongs in a decision, not an end-of-run addition.
  - **Follow-ups for Gemini**: run `/impeccable adapt` on the Owner portal, which keeps 16px card radii and 2px icons and will visibly diverge from the console; decide the profile-draft question; decide whether `src/components/ui/dialog.tsx` should lose `backdrop-blur-xs` on its scrim (shared with the Owner portal, so it needs an Owner modal in front of whoever changes it).
```

---
---

# Owner Portal (second paste block, same file)

Insert the LOCKED entry into the **LOCKED CONTRACT REGISTRY** and the ledger
entry under **Model A (Claude) Blueprint & Directives**, below the Dispatcher
entries above.

---

## For the LOCKED CONTRACT REGISTRY

```markdown
* `[LOCKED]` `Owner Portal Skin (second skin on the Route Board system)`:
  - **One grammar, two skins.** The Owner Portal does NOT have its own design system. It inherits the Dispatcher's Route-Board system whole - the seven type roles with a 12px floor, the three radii, the five status tones, the two line weights, `--ease-board`, the SUGO red signal, the primitive contracts, the honesty rules - and differs in exactly **nine colour declarations and one rule** (eight decisions: `--color-hairline` and `--color-edge` are the two halves of one line-weight relationship and are never chosen separately). Anything not in the list below is shared, and changing it changes both portals.
  - **Skin home**: `src/styles/owner.css`, scoped to `[data-surface="owner"]`. `--color-board-field` #1E3A5F (the navy that was hardcoded 120 times while the token was consumed zero times), `--color-board-field-deep` #162D4A, `--color-board-trim` #98A7BB, `--color-board-ground` #F1F5F9, `--color-board-plate` #FFFFFF, `--color-ink` #0F172A, `--color-ink-muted` #475569, `--color-hairline` rgb(100 116 139 / 0.22), `--color-edge` rgb(100 116 139 / 0.40).
  - **`--color-board-trim` is #98A7BB and NOT slate-400.** Measured: slate-400 is 4.49:1 as text on the navy field and fails AA by one hundredth. Do not "simplify" it back to a palette step. The comment in `owner.css` says so; keep it.
  - **The ground is #F1F5F9, not #F8FAFC.** With no shadows, a white plate on #F8FAFC is a 1.5% luminance step and effectively invisible. The ground change IS the structure here, so it has to be visible.
  - **Two ink levels, deliberately no third.** A third lighter level is how `text-slate-400` at 2.83:1 reached 142 places. `--color-board-trim` is the ONLY on-field text role; there is no second one.
  - **This surface spends no elevation as structure, and the stylesheet enforces that rather than asserting it.** `owner.css` sets `--tw-shadow: 0 0 #0000` for `.shadow-plate` and `.shadow-field` under `[data-surface="owner"]`, because `shadow-plate` does NOT compile to `var(--shadow-plate)` - Tailwind emits `--tw-shadow: 0 1px 2px 0 var(--tw-shadow-color,#0f20350f)` - so the skin cannot retire it by redefining a token the way it redefines the other nine. Rings compose separately and are untouched, so focus rings still land. **This is a guard, not a repair**: no Owner file imports `DispatcherCard` today, but it is the shared card primitive and adopting it is the obvious next refactor, at which point every plate would silently acquire a shadow. Three deliberate exceptions opt back in with `data-elevate`, all of them content floating over something rather than structure: the notification popover, and two controls plus the rider name tag over map imagery.
  - **The re-skin mechanism, and why it cost nine declarations instead of a rewrite.** Every token utility compiles to a `var()` reference and `@theme` emits its variables into a plain `:root,:host` rule. Custom properties resolve **per element**, so a descendant carrying `data-surface="owner"` that redefines a property overrides it for its whole subtree. `:root,:host` can never match a div inside `<body>`, so specificity never arbitrates and no cascade layer is involved. **Zero edits were needed to the 51 already-reviewed Dispatcher files.** Do not reach for a layer, an `!important` or a fork when adding a third skin; add a `data-surface` value.
  - **`src/styles/dispatch.css` no longer exists.** It is `src/styles/surfaces.css`, keyed on bare `[data-surface]` so both skins get the focus ring, `::selection`, caret, real scrollbars, tabular figures, the 1.5px icon stroke and the one motion curve from one place. The split was a selector broadening, so the Dispatcher kept every rule it had. **It is deliberately UNLAYERED and therefore outranks Tailwind utilities** - measured, that is what lets the focus ring survive an element's own `outline-none`. Ground and ink are the exception and sit in `@layer base` so a component's own `bg-*` can win. Anything added here that a component may need to override must go in `@layer base`.
  - **`data-surface="owner"` is required on portalled content** (dialogs, modals, overlays). They render at `document.body`, outside the portal element, so without it they resolve the Dispatcher's values. `/places` is a top-level route outside the portal shell and carries the attribute itself for the same reason.
  - **The primitives moved.** `src/portals/dispatcher/components/ui/` is now `src/components/panel/`, with portal-neutral aliases in `index.ts` (`PanelCard`, `PanelButton`, `StatusChip`, `InlineBanner`, `SearchField`) and the original `Dispatcher*` symbols still exported. `src/lib/statusPresentation.ts` moved to `src/lib/` for the same reason. 26 import sites were rewritten; no symbol was renamed.
  - **`PanelState` orders `error` before `isEmpty`.** That ordering is what makes a false all-clear structurally impossible, and it is why a dead endpoint can no longer render "No user accounts exist yet in the database." It gained an optional `errorBody` this round, because a caller whose `errorTitle` matched its hook's message printed the same sentence twice and nothing flagged it.
  - **A count nobody received is a dash, never a zero.** Every rendered count on a screen routes through ONE unknown-judgement for that screen (`fig()`, `countsUnknown`, or a guarded helper). This is load-bearing and it regressed four separate times during the build, each time as a single indicator left on the raw value beside guarded siblings: a roster tab reading "Signal (0)" under a panel saying the roster did not load; a toolbar reading "0 of 0 accounts shown" under "none arrived"; an archive header reading "(0)" under its own failure notice; and a category card reading "0 Stores" because `?? 0` closed the expression. **The test is a grep of every rendered count against that one helper, not a reading of the error path.**
  - **Two glyphs for two absences**: an ellipsis means still arriving, a dash means never arrived, and each carries a `title` saying which. A zero claims the operation did nothing.
  - **A period change clears its figures in the same beat.** `useDashboardMetrics` and `useReport` call `setData(null)` at the top of every read. Without it `data` survived the change and, because every figure renders as `data ? value : placeholder`, the placeholder machinery could never fire again after first mount: switching Today to Month kept LAST period's numbers beside a heading that already said Month, and a failed request left them there. Do not remove those lines as a wasteful re-render.
  - **`StandingFigures` is the one device for stating a live figure** on this surface - on the navy field, divided by column rules between cells, with `data-on-field` inverting the chrome. Used by the dashboard (3 columns), Tracking (4) and Riders (3). It replaced three separate rows of identical metric tiles.
  - **`MetricCard` has no `color` prop and must not regain one.** It took `color: string`, which 27 call sites filled with 12 raw hexes, and a template-literal background minted a tinted icon chip from whatever arrived. `tone` names a MEANING from the status law instead, and most figures correctly pass none. The tinted icon chip is banned outright - it was the single most category-generic element in this codebase, and 25 of them are gone.
  - **The Sidebar Alignment Contract is unchanged and re-verified in the browser**, both states, measurement for measurement: expanded rail 256px, row box 12 to 243, icon axis 24, icon size 16 (the lucide `size` prop is inert), label axis 50, group labels at 12; collapsed rail 56px, 36x36 tiles at 10 to 46, centred on axis 28. The two `SidebarTrigger`s are intentional, one per state.
  - **Sidebar and header WORDING is frozen by instruction.** Every nav label, group label, brand string, strapline and module title ships byte-identical to before this redesign. Appearance was in scope; the strings are not.
```

---

## For Model A (Claude) Blueprint & Directives

```markdown
* **Owner Portal Redesign (Impeccable, second skin on the Route Board, no direction roll)**:
  - Full remodel of `src/portals/owner/**` (31 files, roughly 9,700 lines) under the recorded contract at `.impeccable/surfaces/src-portals-owner-ownerportal-tsx.md`. **No concept tournament was run, and that is contract-correct**: the Impeccable flow routes a whole surface inside an established world to keep the visual system fixed. The Route Board already governs this codebase, is documented, and has been finish-reviewed. This adds a second skin to that world, not a second world to the product.
  - **Zero-Backend Invariant honoured throughout.** No Express, controller, Prisma, migration or query touched. `apiService.ts` and `apiClient.ts` were read-only for the run; where a service returns `null` on failure the fix went in the caller. Every prop and handler name is unchanged. `useRiderFleetPresence`'s socket, Firebase subscription and 15s poll are intact; its `loadError`/`reload` and `useReport`'s `reload` were added **additively** to the returned object.
  - **The headline defect: the portal could tell an owner that a rejected write had succeeded.** `apiService.updateRateConfig` returns `null` on failure and `ServiceRatesModule` ignored the return value, so a success toast fired on a **rejected write to the pricing every customer pays**. It now checks the result and says plainly that the server did not accept the change. When the config fails to *load*, the form no longer shows hardcoded seed values as though they were saved config, and Save is disabled while the load is broken.
  - **8 swallowed catches to 0** (6 now set real UI state; 1 is a deliberate log-only with a documented honest fallback, on a cosmetic rates fetch whose labels stop quoting figures they do not have). **5 native dialogs to 0** - `window.confirm` and `alert()` are gone, replaced by `ConfirmDialog` and in-page banners; the delete-failure path previously had no in-page error state at all.
  - **894 lines of duplication deleted.** `PlacesDirectoryScreen` was 953 lines, 894 of them identical to `PlacesTab`, **unreachable from inside the portal** because both entry links are gated off, and already diverged over whether retired stores appear in the directory. It is now a 75-line route shell around the one component, settled on the documented behaviour. Three dead files carrying fabricated figures were deleted - zero importers, and a `[LOCKED]` Zero-Slop breach sitting in the tree.
  - **The dashboard was recomposed, not restyled.** Six identical tiles differentiated only by a hex became three regions by time base: a navy standing-figures band for what is true right now, a period ledger setting money against the trend that produced it, and the all-time count demoted to a footnote that says it ignores the period selector. `RevenueChart`'s "LIVE SYNC" badge is gone - it was lit unconditionally, so it announced a live feed over failed fetches.
  - **Verification ledger**: `npx tsc --noEmit` 0 errors; `npm run build` clean, OwnerPortal chunk 246.45 kB / 55.31 kB gzip (from 272.04 / 59.08); `impeccable detect` **0 findings repo-wide**, from a baseline of 9; 0 unused locals or parameters in `src/portals/owner/**` and `src/components/panel/**`. Measured to zero across the portal: banned shadow levels, `backdrop-blur`, gradients, `active:scale`, legacy radii, purple/violet/indigo/cyan/sky, arbitrary pixel font sizes, synthetic italics, emoji, rendered prose em dashes, hardcoded navy hexes. Now consumed: **1,455 design tokens, 504 type-role utilities, 255 system radii**, from zero of each. WCAG: 20 token pairs computed, 0 failures; tightest are `board-trim` on the field at 4.70:1 and white on `signal` at 4.83:1.
  - **8 pre-existing unused-code findings remain in shared files** (`App.tsx`, `ErrorBoundary`, `PlacesMiniMap`, `StaffAvatar`, `popover`, `sidebar`, and two in `useRiderFleetPresence` at the same line numbers they occupy in `HEAD`). Left alone deliberately: that hook is shared by both portals and the invariant requires data hooks stay intact.
  - **Unverified, and it matters**: the API is unreachable from the dev machine (CORS against `api.sugo-express.org`; the local server answers 401 with stale seed credentials), so **no state in this build has been seen against production records**. All fourteen screenshots in `.impeccable/review/` are failure or empty states, which makes the error paths unusually well covered and the populated paths not covered at all. Captures were taken with non-localhost hosts blocked at the resolver, because the socket, Firebase and Maps retry loops otherwise stall Chrome's virtual clock; a side effect is that both map surfaces render their own map-failure state. Chrome's headless window clamps to a 500px minimum here, so the 390px case is measured numerically. There is **no test infrastructure in this repo**, so the automated safety net is the four gates above and nothing else.
  - **Two finish-review rounds ran; the reviewer was spawned fresh with no inherited transcript both times.** Round 1 returned `fix` with six findings, all acted on. The two that mattered: the direction contract's named signature interaction did not exist - not as a missing feature but as honest placeholder machinery wired to a state that could no longer reach it - and the arrangement the THESIS refuses by name (a row of identical metric tiles above a table) survived on every screen that had not adopted the shell. Round 2 confirmed five fixed, **withdrew one after I disputed it** (it had asserted `DispatcherCard` was consumed by five Owner screens; measured, no Owner file imports it at all), and caught **three regressions I had introduced while fixing**: the new map-failure scrim painted its own controls as unclickable ghosts; the duplicate-sentence defect I fixed on Riders was live on Users and Merchants; and the Places consolidation carried the wrong noun into its failure panel, because one `loadError` served two different fetches and the later one overwrote the earlier. All three are fixed.
  - **Not done, and it was asked for.** The metric-tile row survives on `ExceptionReportView` and `RiderPerformanceReportView`. I removed it from `SalesReportView`, `CommissionReportView` and `SettlementReportView`, where the figures were genuinely printed twice or three times on one screen because the tables below carry their own bold Total rows, and kept it on the two where the tiles are the only statement of a real summary and the tones carry meaning. That is a judgement, not a completion - a reviewer asked for all five.
  - **Known incomplete, stated plainly**: 10 of 50 form controls are label-wired (all ten in `ServiceRatesModule`, all pairing exactly); `aria-describedby` and `aria-invalid` are **0 portal-wide**, so roughly 38 validation messages are not linked to the inputs they describe; five hand-rolled overlays still lack `role="dialog"`, `aria-modal`, focus trap, focus restore and Escape (`MerchantCategoryModule`, `PlacesTab`, `AddUserModal`, `EditUserModal` twice) while `ConfirmDialog` is built and proven; and Users, Merchants and Tracking keep their own header row because their bodies are `flex-1` chains around a map or a dual-view pane. Those three now carry the same `h1` treatment on the same hairline as the five shell screens, so the portal has one header **treatment** on eight screens and one header **component** on five, with four mounts of `HeaderClock` and `NotificationBell` rather than one.
  - **A self-inflicted regression worth recording as a warning.** One sweep applied an unconditional whitespace collapse to whole lines rather than only to the class strings it was editing, flattening leading indentation on **8,002 of 8,724 non-blank lines across 31 files**. `tsc` stayed green throughout, because whitespace is invisible to the type checker. Repaired with `npx prettier` (no dependency added) plus a comment-continuation re-indenter. A second sweep replaced the word `border` wherever it appeared on a matching line, including object property keys (producing a syntax error) and three English sentences inside comments. **A green typecheck is not evidence that an automated refactor was safe.** Any future sweep must match inside the string literal it intends to edit, never the whole line.
  - **Follow-ups for Gemini**: wire the remaining 40 form controls through the `Field` primitive, which cannot render without linking its label; migrate the five hand-rolled overlays onto the `Dialog` primitive (`EditUserModal`'s re-auth step is an early `return` that REPLACES its parent rather than stacking on it, so it migrates last and alone); give Users, Merchants and Tracking the `OwnerPanelShell` once their flex chains are restructured; and route each Dispatcher panel's primary list through the errand-plate treatment, which that portal's finish review asked for and neither round has done. One more, found in the Dispatcher while proving the Owner skin had not leaked: its queue tabs render `INCOMING 0` / `ACTIVE 0` beside a panel saying "The board did not load", which is the same confident-zero-over-a-failure class this Owner round eliminated four times. Left untouched deliberately - that portal is already finish-reviewed and was out of scope here - but it is a real honesty defect and the `fig()` pattern in `RiderTrackingModule` is the fix to copy.
  - **Before committing**: `git checkout -- src/app/routes.tsx` removes the three temporary unprotected verification routes (`/__owner`, `/__dispatch`, `/__places`). They exist because the seeded staff credentials no longer log in. They must not ship.
```
